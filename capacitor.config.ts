import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'T1_1_Irene_Villa',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
